# pip statements to install all used modules
*pip install cma*  <-- Installs Covariance Matrix Adaptation module

*pip install pyglet*

*pip install pymunk* <-- Installs pymunk simulation package
